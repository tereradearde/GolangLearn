run:
  timeout: 5m
  tests: true

linters:
  enable:
    - govet
    - revive
    - errcheck
    - staticcheck
    - gofmt
    - goimports
    - gosimple
    - ineffassign
    - gocyclo
    - misspell

issues:
  exclude-use-default: false
  max-issues-per-linter: 0
  max-same-issues: 0

